# This script analyzes the accuracy of previous runs based on log files. 
# It reads all .log files, extracts the maximum accuracy (Max acc:) from each file
# Due to space constraints, the results for Reddit were not included in the paper.
python3 ../acc.py
